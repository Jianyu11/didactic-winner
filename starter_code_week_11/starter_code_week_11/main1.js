import { age, height } from "./main";

console.log('this works')

let noun = "heart";
let verb = "run";
let adjective = "possible";
let sentence = `my ${noun} leaps ${adjective} when I ${verb} a rainbow in the sky`;if (age < 8) {
    console.log('check out the merry go round');
} else if (age > 8 && age < 65 && height);

